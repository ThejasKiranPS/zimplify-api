export enum ProjectStatus {
  deploying = "Deploying",
  deployed = "Deployed",
  failed = "Failed"
}

export enum IntegrationPlatform {
  GITHUB = "GITHUB",
}

export enum ProjectSourceType {
  GITHUB = "GITHUB",
  GITLAB = "GITLAB",
  BITBUCKET = "BITBUCKET",
  MANUAL = "MANUAL"
}